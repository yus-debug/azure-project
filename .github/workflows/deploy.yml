name: CI/CD to Azure

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build React app
        run: npm run build

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          build-args: |
            VITE_SUPABASE_URL=https://pjcaduizojptiijrtjwx.supabase.co
            VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBqY2FkdWl6b2pwdGlpanJ0and4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzcxOTIwMDMsImV4cCI6MjA5Mjc2ODAwM30._-3krfGSTfbouWAHIfkD5PB2sW8-igvE9fFKrWeXNJ8
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/skill-tree:latest
            ${{ secrets.DOCKER_USERNAME }}/skill-tree:${{ github.sha }}